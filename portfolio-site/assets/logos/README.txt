Drop your logo files here using these exact filenames:

  uvidnova.png   →  uVidNova (UVN)
  modelup.png   →  ModelUp (MUP)
  roofsolar.png   →  RoofSolar (RSL)
  polymind.png   →  PolyMind (PMD)
  dishroll.png   →  DishRoll (DSH)
  mortwise.png   →  MortWise (MWS)
  storyroute.png   →  StoryRoute (SRT)

Notes:
- PNG recommended. Square works best. Transparent background ideal.
- SVG also works — rename the line above from .png to .svg and update index.html.
- If a logo file is missing, the ticker (e.g. UVN) shows as a styled fallback automatically.
